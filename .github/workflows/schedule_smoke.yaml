name: schedule-smoke

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: |
          date -u
          echo "event=${{ github.event_name }}"
